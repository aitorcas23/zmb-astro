---
interface Props {
	extraClass?: string | { [key: string]: any };
}

const { extraClass } = Astro.props;
---

<h1
	class:list={[
		"text-center text-4xl font-extrabold uppercase text-gray-900 lg:text-5xl",
		extraClass,
	]}
>
	<slot />
</h1>
